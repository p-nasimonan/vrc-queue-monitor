apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vrc-queue-monitor
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"

    ## argocd-image-updater
    ## backend は api/collector で同じイメージを使うので1エイリアスにまとめる
    argocd-image-updater.argoproj.io/image-list: >-
      backend=ghcr.io/p-nasimonan/vrc-queue-monitor/backend,
      frontend=ghcr.io/p-nasimonan/vrc-queue-monitor/frontend

    argocd-image-updater.argoproj.io/backend.update-strategy: newest-build
    argocd-image-updater.argoproj.io/frontend.update-strategy: newest-build

    ## mainブランチのCIが付与する v0.<sha> タグのみ対象
    argocd-image-updater.argoproj.io/backend.allow-tags: regexp:^v[0-9]
    argocd-image-updater.argoproj.io/frontend.allow-tags: regexp:^v[0-9]

    ## write-back: Application の helm.parameters に書き込む
    ## auto-sync が有効なら自動的に再デプロイされる
    argocd-image-updater.argoproj.io/write-back-method: argocd

    ## エイリアス名と一致させること（alias.helm.image-tag）
    argocd-image-updater.argoproj.io/backend.helm.image-tag: backend.image.tag
    argocd-image-updater.argoproj.io/frontend.helm.image-tag: frontend.image.tag

spec:
  project: default
  source:
    repoURL: https://github.com/p-nasimonan/vrc-queue-monitor.git
    targetRevision: HEAD
    path: charts/vrc-queue-monitor
    helm:
      releaseName: vrc-queue-monitor
      valueFiles:
        - values.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: vrc-monitor

  syncPolicy:
    ## automated を有効にしないと Image Updater の write-back 後に sync されない
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
